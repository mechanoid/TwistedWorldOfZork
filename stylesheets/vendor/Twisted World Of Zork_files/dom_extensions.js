// Generated by CoffeeScript 1.6.1
(function() {

  HTMLElement.prototype.hasClass = function(name) {
    return new RegExp("(^|\\s)" + name + "(\\s|$)").test(this.className);
  };

  HTMLElement.prototype.addClass = function(name) {
    if (!this.hasClass(name)) {
      return this.className += " " + name;
    }
  };

  HTMLElement.prototype.removeClass = function(name) {
    return this.className = this.className.replace(new RegExp(" ?" + name), '');
  };

  Array.prototype.addClass = function(name) {
    var entry, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      entry = this[_i];
      _results.push(entry.addClass(name));
    }
    return _results;
  };

  Array.prototype.removeClass = function(name) {
    var entry, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      entry = this[_i];
      _results.push(entry.removeClass(name));
    }
    return _results;
  };

}).call(this);
